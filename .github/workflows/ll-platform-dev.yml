name: Build ll-platform-dev

on:
  workflow_dispatch:
  push:
    branches:
      - main
      - "releases/**"
    paths:
      - environments/dev

env:
  major: 0
  minor: 0

jobs:
  setup:
    runs-on: ubuntu-latest
    outputs:
      version: ${{ steps.set_version.outputs.version }}
      tag: ${{ steps.set_tag.outputs.tag }}

    steps:
      - uses: actions/checkout@v4

      - name: Create version variable
        id: set_version
        shell: bash
        run: |
          # Read the version from the file
          version=$(cat versions/ll-platform-dev.version)
          echo "Current version is $version"

          # Split the version into major, minor, and patch
          IFS='.' read -r major minor patch <<< "$version"

          # Increment the patch number
          patch=$((patch + 1))

          # Construct the new version
          new_version="${major}.${minor}.${patch}"
          echo "New version is $new_version"

          # Set the version as an output and environment variable
          echo "version=$new_version" >> $GITHUB_OUTPUT
          echo "version=$new_version" >> $GITHUB_ENV

      - name: Set tag variable
        id: set_tag
        shell: bash
        run: |
          echo "GITHUB_REF is $GITHUB_REF"
          version="${{ steps.set_version.outputs.version }}"
          if [[ "$GITHUB_REF" == "refs/heads/main" ]]; then
            tag="main-${version}"
          elif [[ "$GITHUB_REF" == refs/heads/releases/* ]]; then
            tag="release-${version}"
          else
            tag="${version}"
          fi
          echo "Tag is $tag"
          echo "tag=$tag" >> $GITHUB_OUTPUT

      - name: Set tagid and tagcommit
        id: set_tagid
        run: |
          echo "tagid=build-${{ github.run_id }}" >> $GITHUB_OUTPUT
          echo "tagcommit=commit-${{ github.sha }}" >> $GITHUB_OUTPUT

  helm:
    needs: setup
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Helm Package and Push
        uses: ./.github/actions/build-and-push-helm
        with:
          version: ${{ needs.setup.outputs.version }}
          tag: ${{ needs.setup.outputs.tag }}
          workingDirectory: "environments/dev/ll-platform-dev"
          token: ${{ secrets.GITHUB_TOKEN }}
          imageName: "ll-platform-dev"

  bump_version:
    runs-on: ubuntu-latest
    needs:
      - setup
      - helm
    steps:
      - uses: actions/checkout@v4
      - name: Update version file
        shell: bash
        run: |
          # Update the version in the file
          echo "${{ needs.setup.outputs.version }}" > versions/ll-platform-dev.version
          # Commit and push changes
          git config user.name "${{ github.actor }}"
          git config user.email "${{ github.actor }}@users.noreply.github.com"
          git add versions/ll-platform-dev.version
          git commit -m "Bump ll-platform-dev version to ${{ needs.setup.outputs.version }}"
          git push
